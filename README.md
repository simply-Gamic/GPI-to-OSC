# GPItoOSC
A repo which takes any GPI input and converts them to OSC messages. The library can be used and adopted for any use case but the example is built around a RasPi which takes GPI input and converts them to OSC for use in TallyArbiter
